import Layout from "/components/layout";
import styles from "/styles/home.module.css";

const HomePage = () => (
  <Layout>
    <h3 className={styles.event}>Join us November 20th</h3>
  </Layout>
);

export default HomePage;
